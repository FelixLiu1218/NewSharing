<UserControl x:Class="Sharing.UserPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sharing"
             xmlns:core="clr-namespace:Sharing.Core;assembly=Sharing.Core"
             d:DataContext="{x:Static local:TextEntryDesignModel.Instance}"
             mc:Ignorable="d">
    

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
            
        <TextBlock Style="{StaticResource UserIcon}" VerticalAlignment="Center"
                   Margin="15 5 5 0" />
        <TextBlock  Grid.Column="1" Style="{StaticResource TopUserName}" Text="{Binding OriginalText}" VerticalAlignment="Center"
                    Margin="5 5 0 0" />
            
    </Grid>
</UserControl>
